My Novel:
  content: novel/c/**/*.md
  formats: docx, epub
  output: novel/out/${name}.${format}

Blog Posts:
  type: individual
  content: blog/**/*.md
  formats: html
  output: ${content.dir}/${name}.${format}
